services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "5173:5173"
    networks:
      - app-network

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "5000:5000"
    networks:
      - app-network
    environment:
      - GROQ_API_KEY=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
      - PROMPT_ROOT_DIR=/app/prompt_tests/omnicomplete
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 5

networks:
  app-network:

